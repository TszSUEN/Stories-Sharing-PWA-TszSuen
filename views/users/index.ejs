<h2 class="page-header">Search Users</h2>
<form action="/users" method="GET">
  <div class="form-row">
    <div class="form-item">
      <label>User Name</label>
      <input type="text" name="username" value="<%= searchElements.username %>">
    </div>
  </div>
  <div class="form-row form-row-end">
    <button class="btn btn-primary" type="submit">Search</button>
  </div>
</form>
<br>

<% users.forEach(user => { %>
  <div class="user-row">
    <div class="user-name"><%= user.username %></div>
    <div class="btn-row">
      <a class="btn btn-primary" href="/users/<%= user.id %>">View</a>

<!--  this part is for admin permission &#45;&#45; edit/ delete a user information-->
<!--      <a class="btn btn-primary" href="/users/<%= user.id %>/edit">Edit</a>-->
<!--      <%- include('../sectionals/delete_form', { url: `/users/${user.id}` }) %>-->

    </div>
  </div>
<% }) %>